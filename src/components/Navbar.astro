<style>
  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 5vw; /* Viewport genişliğine göre yatay padding */
    background-color: #ffffff73;
    backdrop-filter: blur(10px);
    border-bottom: 1px solid #dddddd00;
    position: sticky;
    top: 0;
    height: 10vh; /* Viewport yüksekliğine göre */
    min-height: 70px; /* Minimum yükseklik garantisi */
    max-height: 100px; /* Maksimum yükseklik sınırı */
    z-index: 999;
    margin-bottom: 50px;
  }

  .logo {
    font-weight: bold;
    display: flex;
    align-items: center;
    height: 100%;
  }

  .logo img {
    height:auto; /* Container yüksekliğinin %80'i */
    width: 70%; /* Orantılı genişlik */
    max-width: 35vw; /* Viewport genişliğinin %35'i kadar */
    min-width: 150px; /* Mutlak minimum genişlik */
    border-radius: 8px;
    object-fit: contain;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
  }

  .nav-links a {
    text-decoration: none;
    color: #283389;
    font-weight: 500;
    font-family: 'DynaPuff', sans-serif;
    font-size: clamp(0.8rem, 1.5vw, 1rem); /* Responsive font boyutu */
  }
 
  .menu-toggle {
    display: none;
    font-size: 1.5rem;
    cursor: pointer;
    background: none;
    border: none;
    color: #283389;
  }

  @media (max-width: 768px) {
    nav {
      padding: 1rem 5vw;
      height: 8vh;
      min-height: 60px;
    }

    .logo img {
      height: auto;
      max-width: 60vw;
    }

    .nav-links {
      display: none;
      flex-direction: column;
      gap: 1rem;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: white;
      padding: 2vh 5vw;
      border-top: 1px solid #ccc;
    }

    .nav-links.open {
      display: flex;
    }

    .menu-toggle {
      display: block;
    }
  }

  /* Çok küçük ekranlar için */
  @media (max-width: 480px) {
    .logo img {
      max-width: 60vw;
      min-width: 220px;
    }
    
    .nav-links a {
      font-size: clamp(0.9rem, 4vw, 1rem);
    }
  }
</style>

<nav>
  <div class="logo">
    <a href="/"><img src="public/images/yazilimnealaka-logo-500-line.png" alt="Yazılım Ne Alaka Logo" /></a>
  </div>

  <button class="menu-toggle" onclick="document.querySelector('.nav-links').classList.toggle('open')">
    ☰
  </button>

  <div class="nav-links">
    <a href="/neden">Neden Yazılım Ne Alaka?</a>
    <a href="/gizli-sayfa">Gizli Sayfa</a>
    <a href="/bugun-ne-ogrendim">Bugün Ne Öğrendim?</a>
    <a href="/yapim-asamasinda">Sonraki Bölümler</a>
  </div>
</nav>